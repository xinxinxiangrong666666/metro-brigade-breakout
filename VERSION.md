# 地铁旅团弹射版本记录

## v0.6-city-breakout

冻结时间：2026-08-23

以 v0.5《像素旅团：弹射远征》为玩法底座，主题从北京候机楼换皮为深圳地铁城市突围，
并修复五站链路、背景映射、方块贴图、失败重试、结算时机等玩法问题。

### 主题换皮

- 封面改为深圳启动页，含加载进度条
- 新增"城市突围线路"页：五站进度节点 + 明信片计数 + 当前任务
- 关卡名：高新园 1-1 / 南头古城 1-2 / 前海湾 1-3 / 前海石公园 1-4 / 深中通道 1-5
- 事件名：早高峰顺风 / 古城回响 / 海风放行 / 旅团共鸣
- 过场文案五段，每站一段，与站点一一对应

### 玩法链路修复

- `STAGES` 从 3 关扩为 5 关，与 `ROUTE_STOPS` 严格 1:1
- `routeProgress = level` 始终同步，删除 `level === 2 ? 2 : 4` 等硬编码 hack
- 修复第 5 站（深中通道）永远打不到：`startCurrentRoute` 的 `>= 5` 改为 `> 5`
- 修复 `level` 溢出到 6 导致 `STAGES[5]` 越界：终局不再进升级流程
- 高新园与前海湾两站明信片此前永远拿不到，现已可正常收集

### 背景与美术

- 五关背景各自独立，`drawCityBackground()` 逐关分发（原为 `level<=2 / <=4 / else` 粗分段）
- 方块从程序化色块 + 英文标签改回 v0.5 的 atlas 随机像素贴图
- `drawBlock()` 与 v0.5 逐字符等价（含金色技能箱双层光环、半血裂痕描边）

### 体验修复

- **体力归零保留进度**：失败回线路图重挑本站，不再清空跳回第 1 关；通关才整局重置
- **方块清零即刻结算**：原逻辑要等在飞弹珠全部消亡（最长 11.5 秒），屏幕已空却无反应
- 技能面板可折叠，折叠后遮挡面积减少 95.9%（443×241 → 100×43）
- 技能详情弹窗在面板折叠时自动关闭

### 工程结构

- 从 19.9 MB 单文件 `index.html` 拆分为 v0.5 式源码结构
- `index.html` 9.5 KB / `styles.css` 28 KB / `src/game.js` 64 KB / `assets/` 12 张 PNG
- 12 处内联 base64（JS 8 / CSS 4）与 1 处内联 `<img>` 全部还原为外部文件

## v0.5-departure-story（底座）

北京候机楼主题。弹射统一解释为"为航班清出起飞航道"，含候机楼像素背景、
开场倒计时钩子、每关一次性飞机起飞过场、通关主题升华文案。

## v0.4-skill-clarity

技能可读性：技能槽可查看完整说明，三选一卡片显示本次提升、具体规则、选择前后数值。

## v0.3-current-snapshot

手机横屏 H5 三关玩法框架、单指跟手瞄准与多段反弹预览、三角色特性、
像素方块图集与金色技能箱、六项技能、粒子/伤害/连击/音效/震动/暂停/横竖屏适配。
